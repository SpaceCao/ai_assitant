# HiAgent意图识别测试用例表

用于 HiAgent 意图识别流程测试，当前测试口径严格对齐：

- `1.3 Agent 与业务中心映射`
- `2.0 意图关键词`
- `2.3 HiAgent 智能体协作关系图 / 协作模式`

## 1. 测试目标

验证以下内容：

1. Python 前置节点是否正确返回 `intents`
2. 大模型输出的 `intent_code` 是否符合 2.0 正式定义
3. 大模型输出的 `target_agent` 是否符合 1.3 正式归属
4. 大模型输出的 `collaboration_mode` 是否符合 2.3 协作模式
5. 模糊输入是否优先进入澄清或闲聊兜底

## 2. 测试前置条件

每次回归前，先确认：

1. Python 前置节点 `intent_mapping_workflow` 已执行成功
2. 前置节点输出中存在 `intents`
3. 大模型节点用户提示词已正确引用 `{{intent_mapping_workflow.intents}}`
4. 选择器按 `need_clarify` 分流

## 3. 高优先级回归样例

| 用例ID | 用户输入 | 预期意图 | 预期 intent_code | 预期 target_agent |
|--------|----------|----------|------------------|-------------------|
| TC001 | 帮我推荐点零食 | 商品推荐 | `view_items` | 商推Agent |
| TC002 | 送长辈买什么 | 商品推荐 | `view_items` | 商推Agent |
| TC003 | 看看我的购物车 | 查询购物车 | `view_shop_cart` | 交易Agent |
| TC004 | 购物车里有什么 | 查询购物车 | `view_shop_cart` | 交易Agent |
| TC005 | 这个加购物车 | 添加购物车 | `add_shop_cart` | 交易Agent |
| TC006 | 把刚才推荐的都加上 | 添加购物车 | `add_shop_cart` | 交易Agent |
| TC007 | 把这个从购物车删掉 | 删除购物车 | `del_shop_cart` | 交易Agent |
| TC008 | 清空购物车 | 清空购物车 | `clear_shop_cart` | 交易Agent |
| TC009 | 确认订单 | 确认订单 | `confirm_order` | 交易Agent |
| TC010 | 直接下单 | 立即下单 | `place_order_oow` | 交易Agent |
| TC011 | 我要取消订单 | 取消订单 | `cancel_order` | 交易Agent |
| TC012 | 我的订单 | 查询订单 | `query_order` | 交易Agent |
| TC013 | 订单状态怎么样 | 查询订单 | `query_order` | 交易Agent |
| TC014 | 会员有什么权益 | 知识问答 | `knowledge_quiz` | 问答Agent |
| TC015 | 这个配料是什么 | 知识问答 | `knowledge_quiz` | 问答Agent |
| TC016 | 运费怎么算 | 知识问答 | `knowledge_quiz` | 问答Agent |
| TC017 | 你好 | 闲聊回复 | `casual_reply` | 闲聊Agent |
| TC018 | 谢谢 | 闲聊回复 | `casual_reply` | 闲聊Agent |

## 4. 边界样例

| 用例ID | 用户输入 | 预期意图 | 风险点 |
|--------|----------|----------|--------|
| TC019 | 还差20免运费，帮我凑一下 | 商品推荐 | 容易误判为知识问答 |
| TC020 | 这个适合老人吗 | 知识问答 | 容易误判为商品推荐 |
| TC021 | 就这些，确认一下 | 确认订单 | 容易误判为立即下单 |
| TC022 | 现在就买 | 立即下单 | 容易误判为确认订单 |
| TC023 | 帮我退掉这单 | 取消订单 | 容易误判为查询订单 |
| TC024 | 订单到哪了 | 查询订单 | 架构文档 2.0 只保留 `query_order`，当前阶段统一归交易Agent 承接 |
| TC025 | 会员日有什么活动 | 知识问答 | 容易误判为商品推荐 |

## 5. 多意图样例

| 用例ID | 用户输入 | 预期主意图 | 预期 secondary_intent_codes | 说明 |
|--------|----------|------------|-------------------------|------|
| TC026 | 帮我推荐点零食，顺便看看购物车 | `view_items` | `view_shop_cart` | 当前优先做推荐，购物车为次级诉求 |
| TC027 | 把这个加购物车，然后直接下单 | `add_shop_cart` | `place_order_oow` | 当前应先完成加购，再处理下单 |
| TC028 | 查一下这个订单，能不能顺便取消 | `query_order` 或澄清 | `cancel_order` | 若订单对象不明确，可转澄清 |
| TC029 | 推荐点适合老人的礼盒，再告诉我配料 | `view_items` | `knowledge_quiz` | 推荐为主，商品知识为次级 |

## 6. 模糊与兜底样例

| 用例ID | 用户输入 | 预期结果 | 说明 |
|--------|----------|----------|------|
| TC030 | 这个 | 需要澄清 | 指代不明 |
| TC031 | 查一下 | 需要澄清 | 诉求不完整 |
| TC032 | 都要 | 需要澄清 | 缺乏上下文 |
| TC033 | 在吗 | 闲聊回复 | 闲聊兜底 |

## 7. 验收标准

- Python 前置节点稳定返回 `intents`
- 高频样例命中率 >= 90%
- `intent_code`、`target_agent`、`collaboration_mode` 输出一致
- 多意图输入能稳定输出一个主意图，且 `secondary_intent_codes` 合理
- 模糊输入优先进入澄清或闲聊兜底
- `reason_text` 应能解释分类依据

## 8. 测试执行建议

执行顺序：

1. 先检查 Python 前置节点输出
2. 再跑高频回归样例
3. 再跑边界样例
4. 再跑多意图样例
5. 最后跑模糊与兜底样例

每次修改 Python 前置配置或大模型提示词后，至少重新回归：

- TC001
- TC005
- TC010
- TC014
- TC019
- TC022
- TC026
- TC027
- TC030

## 9. 缺陷记录模板

| 缺陷ID | 用户输入 | 实际结果 | 预期结果 | 初步原因 | 修复方式 |
|--------|----------|----------|----------|----------|----------|
| BUG001 | 这个适合老人吗 | 商品推荐 | 知识问答 | 边界规则不清 | 补充系统提示词边界规则 |
